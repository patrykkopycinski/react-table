import React from 'react';
import { shallow } from 'enzyme';
import TableColumnHeaderContent from './TableColumnHeaderContent';

it('renders without crashing', () => {
  shallow(<TableColumnHeaderContent />);
});